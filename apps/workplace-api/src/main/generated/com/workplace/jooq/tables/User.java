/*
 * This file is generated by jOOQ.
 */
package com.workplace.jooq.tables;


import com.workplace.jooq.Indexes;
import com.workplace.jooq.Keys;
import com.workplace.jooq.Public;
import com.workplace.jooq.tables.AgentApiKey.AgentApiKeyPath;
import com.workplace.jooq.tables.AiAgentCredential.AiAgentCredentialPath;
import com.workplace.jooq.tables.AssistantConfig.AssistantConfigPath;
import com.workplace.jooq.tables.AuditLog.AuditLogPath;
import com.workplace.jooq.tables.Calendar.CalendarPath;
import com.workplace.jooq.tables.CalendarEvent.CalendarEventPath;
import com.workplace.jooq.tables.Channel.ChannelPath;
import com.workplace.jooq.tables.ChannelMember.ChannelMemberPath;
import com.workplace.jooq.tables.ChatMessage.ChatMessagePath;
import com.workplace.jooq.tables.ChatMessageAttachment.ChatMessageAttachmentPath;
import com.workplace.jooq.tables.ChatThread.ChatThreadPath;
import com.workplace.jooq.tables.ChatThreadMember.ChatThreadMemberPath;
import com.workplace.jooq.tables.ContactEntry.ContactEntryPath;
import com.workplace.jooq.tables.ContactFavorite.ContactFavoritePath;
import com.workplace.jooq.tables.DriveFileRef.DriveFileRefPath;
import com.workplace.jooq.tables.DriveFileVersion.DriveFileVersionPath;
import com.workplace.jooq.tables.DriveShareLink.DriveShareLinkPath;
import com.workplace.jooq.tables.DriveSpace.DriveSpacePath;
import com.workplace.jooq.tables.DriveSpaceMember.DriveSpaceMemberPath;
import com.workplace.jooq.tables.EmailAccount.EmailAccountPath;
import com.workplace.jooq.tables.EventAttendee.EventAttendeePath;
import com.workplace.jooq.tables.File.FilePath;
import com.workplace.jooq.tables.HomeSession.HomeSessionPath;
import com.workplace.jooq.tables.Issue.IssuePath;
import com.workplace.jooq.tables.IssueAssignee.IssueAssigneePath;
import com.workplace.jooq.tables.IssueAttachment.IssueAttachmentPath;
import com.workplace.jooq.tables.IssueComment.IssueCommentPath;
import com.workplace.jooq.tables.IssueDependency.IssueDependencyPath;
import com.workplace.jooq.tables.IssueHistory.IssueHistoryPath;
import com.workplace.jooq.tables.IssueWatcher.IssueWatcherPath;
import com.workplace.jooq.tables.Membership.MembershipPath;
import com.workplace.jooq.tables.Message.MessagePath;
import com.workplace.jooq.tables.MessageAttachment.MessageAttachmentPath;
import com.workplace.jooq.tables.MessageReaction.MessageReactionPath;
import com.workplace.jooq.tables.Notification.NotificationPath;
import com.workplace.jooq.tables.PlatformRole.PlatformRolePath;
import com.workplace.jooq.tables.PlatformUserRole.PlatformUserRolePath;
import com.workplace.jooq.tables.Project.ProjectPath;
import com.workplace.jooq.tables.ProjectMember.ProjectMemberPath;
import com.workplace.jooq.tables.RefreshToken.RefreshTokenPath;
import com.workplace.jooq.tables.Role.RolePath;
import com.workplace.jooq.tables.SavedView.SavedViewPath;
import com.workplace.jooq.tables.Tenant.TenantPath;
import com.workplace.jooq.tables.ThreadReadState.ThreadReadStatePath;
import com.workplace.jooq.tables.User.UserPath;
import com.workplace.jooq.tables.UserApiToken.UserApiTokenPath;
import com.workplace.jooq.tables.UserDashboard.UserDashboardPath;
import com.workplace.jooq.tables.UserGroup.UserGroupPath;
import com.workplace.jooq.tables.UserPriorityItem.UserPriorityItemPath;
import com.workplace.jooq.tables.UserRole.UserRolePath;
import com.workplace.jooq.tables.WikiSpace.WikiSpacePath;
import com.workplace.jooq.tables.WikiSpaceMember.WikiSpaceMemberPath;
import com.workplace.jooq.tables.WorkspaceAssistant.WorkspaceAssistantPath;
import com.workplace.jooq.tables.records.UserRecord;

import java.time.LocalDateTime;
import java.util.Arrays;
import java.util.Collection;
import java.util.List;

import org.jooq.Check;
import org.jooq.Condition;
import org.jooq.Field;
import org.jooq.ForeignKey;
import org.jooq.Identity;
import org.jooq.Index;
import org.jooq.InverseForeignKey;
import org.jooq.Name;
import org.jooq.Path;
import org.jooq.PlainSQL;
import org.jooq.QueryPart;
import org.jooq.Record;
import org.jooq.SQL;
import org.jooq.Schema;
import org.jooq.Select;
import org.jooq.Stringly;
import org.jooq.Table;
import org.jooq.TableField;
import org.jooq.TableOptions;
import org.jooq.UniqueKey;
import org.jooq.impl.DSL;
import org.jooq.impl.Internal;
import org.jooq.impl.SQLDataType;
import org.jooq.impl.TableImpl;


/**
 * This class is generated by jOOQ.
 */
@SuppressWarnings({ "all", "unchecked", "rawtypes", "this-escape" })
public class User extends TableImpl<UserRecord> {

    private static final long serialVersionUID = 1L;

    /**
     * The reference instance of <code>public.user</code>
     */
    public static final User USER = new User();

    /**
     * The class holding records for this type
     */
    @Override
    public Class<UserRecord> getRecordType() {
        return UserRecord.class;
    }

    /**
     * The column <code>public.user.id</code>.
     */
    public final TableField<UserRecord, Long> ID = createField(DSL.name("id"), SQLDataType.BIGINT.nullable(false).identity(true), this, "");

    /**
     * The column <code>public.user.username</code>.
     */
    public final TableField<UserRecord, String> USERNAME = createField(DSL.name("username"), SQLDataType.VARCHAR(50).nullable(false), this, "");

    /**
     * The column <code>public.user.email</code>.
     */
    public final TableField<UserRecord, String> EMAIL = createField(DSL.name("email"), SQLDataType.VARCHAR(255), this, "");

    /**
     * The column <code>public.user.password</code>.
     */
    public final TableField<UserRecord, String> PASSWORD = createField(DSL.name("password"), SQLDataType.VARCHAR(255), this, "");

    /**
     * The column <code>public.user.name</code>.
     */
    public final TableField<UserRecord, String> NAME = createField(DSL.name("name"), SQLDataType.VARCHAR(50).nullable(false), this, "");

    /**
     * The column <code>public.user.is_active</code>.
     */
    public final TableField<UserRecord, Boolean> IS_ACTIVE = createField(DSL.name("is_active"), SQLDataType.BOOLEAN.nullable(false).defaultValue(DSL.field(DSL.raw("true"), SQLDataType.BOOLEAN)), this, "");

    /**
     * The column <code>public.user.created_at</code>.
     */
    public final TableField<UserRecord, LocalDateTime> CREATED_AT = createField(DSL.name("created_at"), SQLDataType.LOCALDATETIME(6).nullable(false).defaultValue(DSL.field(DSL.raw("now()"), SQLDataType.LOCALDATETIME)), this, "");

    /**
     * The column <code>public.user.updated_at</code>.
     */
    public final TableField<UserRecord, LocalDateTime> UPDATED_AT = createField(DSL.name("updated_at"), SQLDataType.LOCALDATETIME(6).defaultValue(DSL.field(DSL.raw("now()"), SQLDataType.LOCALDATETIME)), this, "");

    /**
     * The column <code>public.user.kind</code>.
     */
    public final TableField<UserRecord, String> KIND = createField(DSL.name("kind"), SQLDataType.VARCHAR(16).nullable(false).defaultValue(DSL.field(DSL.raw("'HUMAN'::character varying"), SQLDataType.VARCHAR)), this, "");

    /**
     * The column <code>public.user.personal_assistant_agent_id</code>.
     */
    public final TableField<UserRecord, Long> PERSONAL_ASSISTANT_AGENT_ID = createField(DSL.name("personal_assistant_agent_id"), SQLDataType.BIGINT, this, "");

    /**
     * The column <code>public.user.title</code>.
     */
    public final TableField<UserRecord, String> TITLE = createField(DSL.name("title"), SQLDataType.VARCHAR(100), this, "");

    private User(Name alias, Table<UserRecord> aliased) {
        this(alias, aliased, (Field<?>[]) null, null);
    }

    private User(Name alias, Table<UserRecord> aliased, Field<?>[] parameters, Condition where) {
        super(alias, null, aliased, parameters, DSL.comment(""), TableOptions.table(), where);
    }

    /**
     * Create an aliased <code>public.user</code> table reference
     */
    public User(String alias) {
        this(DSL.name(alias), USER);
    }

    /**
     * Create an aliased <code>public.user</code> table reference
     */
    public User(Name alias) {
        this(alias, USER);
    }

    /**
     * Create a <code>public.user</code> table reference
     */
    public User() {
        this(DSL.name("user"), null);
    }

    public <O extends Record> User(Table<O> path, ForeignKey<O, UserRecord> childPath, InverseForeignKey<O, UserRecord> parentPath) {
        super(path, childPath, parentPath, USER);
    }

    /**
     * A subtype implementing {@link Path} for simplified path-based joins.
     */
    public static class UserPath extends User implements Path<UserRecord> {

        private static final long serialVersionUID = 1L;
        public <O extends Record> UserPath(Table<O> path, ForeignKey<O, UserRecord> childPath, InverseForeignKey<O, UserRecord> parentPath) {
            super(path, childPath, parentPath);
        }
        private UserPath(Name alias, Table<UserRecord> aliased) {
            super(alias, aliased);
        }

        @Override
        public UserPath as(String alias) {
            return new UserPath(DSL.name(alias), this);
        }

        @Override
        public UserPath as(Name alias) {
            return new UserPath(alias, this);
        }

        @Override
        public UserPath as(Table<?> alias) {
            return new UserPath(alias.getQualifiedName(), this);
        }
    }

    @Override
    public Schema getSchema() {
        return aliased() ? null : Public.PUBLIC;
    }

    @Override
    public List<Index> getIndexes() {
        return Arrays.asList(Indexes.IDX_USER_EMAIL_UNIQUE, Indexes.IDX_USER_KIND, Indexes.IDX_USER_USERNAME);
    }

    @Override
    public Identity<UserRecord, Long> getIdentity() {
        return (Identity<UserRecord, Long>) super.getIdentity();
    }

    @Override
    public UniqueKey<UserRecord> getPrimaryKey() {
        return Keys.USER_PKEY;
    }

    @Override
    public List<UniqueKey<UserRecord>> getUniqueKeys() {
        return Arrays.asList(Keys.USER_USERNAME_KEY);
    }

    @Override
    public List<ForeignKey<UserRecord, ?>> getReferences() {
        return Arrays.asList(Keys.USER__USER_PERSONAL_ASSISTANT_AGENT_ID_FKEY);
    }

    private transient UserPath _user;

    /**
     * Get the implicit join path to the <code>public.user</code> table.
     */
    public UserPath user() {
        if (_user == null)
            _user = new UserPath(this, Keys.USER__USER_PERSONAL_ASSISTANT_AGENT_ID_FKEY, null);

        return _user;
    }

    private transient AgentApiKeyPath _agentApiKeyCreatedByFkey;

    /**
     * Get the implicit to-many join path to the
     * <code>public.agent_api_key</code> table, via the
     * <code>agent_api_key_created_by_fkey</code> key
     */
    public AgentApiKeyPath agentApiKeyCreatedByFkey() {
        if (_agentApiKeyCreatedByFkey == null)
            _agentApiKeyCreatedByFkey = new AgentApiKeyPath(this, null, Keys.AGENT_API_KEY__AGENT_API_KEY_CREATED_BY_FKEY.getInverseKey());

        return _agentApiKeyCreatedByFkey;
    }

    private transient AgentApiKeyPath _agentApiKeyUserIdFkey;

    /**
     * Get the implicit to-many join path to the
     * <code>public.agent_api_key</code> table, via the
     * <code>agent_api_key_user_id_fkey</code> key
     */
    public AgentApiKeyPath agentApiKeyUserIdFkey() {
        if (_agentApiKeyUserIdFkey == null)
            _agentApiKeyUserIdFkey = new AgentApiKeyPath(this, null, Keys.AGENT_API_KEY__AGENT_API_KEY_USER_ID_FKEY.getInverseKey());

        return _agentApiKeyUserIdFkey;
    }

    private transient AiAgentCredentialPath _aiAgentCredentialCreatedByFkey;

    /**
     * Get the implicit to-many join path to the
     * <code>public.ai_agent_credential</code> table, via the
     * <code>ai_agent_credential_created_by_fkey</code> key
     */
    public AiAgentCredentialPath aiAgentCredentialCreatedByFkey() {
        if (_aiAgentCredentialCreatedByFkey == null)
            _aiAgentCredentialCreatedByFkey = new AiAgentCredentialPath(this, null, Keys.AI_AGENT_CREDENTIAL__AI_AGENT_CREDENTIAL_CREATED_BY_FKEY.getInverseKey());

        return _aiAgentCredentialCreatedByFkey;
    }

    private transient AiAgentCredentialPath _aiAgentCredentialUserIdFkey;

    /**
     * Get the implicit to-many join path to the
     * <code>public.ai_agent_credential</code> table, via the
     * <code>ai_agent_credential_user_id_fkey</code> key
     */
    public AiAgentCredentialPath aiAgentCredentialUserIdFkey() {
        if (_aiAgentCredentialUserIdFkey == null)
            _aiAgentCredentialUserIdFkey = new AiAgentCredentialPath(this, null, Keys.AI_AGENT_CREDENTIAL__AI_AGENT_CREDENTIAL_USER_ID_FKEY.getInverseKey());

        return _aiAgentCredentialUserIdFkey;
    }

    private transient AssistantConfigPath _assistantConfig;

    /**
     * Get the implicit to-many join path to the
     * <code>public.assistant_config</code> table
     */
    public AssistantConfigPath assistantConfig() {
        if (_assistantConfig == null)
            _assistantConfig = new AssistantConfigPath(this, null, Keys.ASSISTANT_CONFIG__ASSISTANT_CONFIG_AGENT_USER_ID_FKEY.getInverseKey());

        return _assistantConfig;
    }

    private transient AuditLogPath _auditLog;

    /**
     * Get the implicit to-many join path to the <code>public.audit_log</code>
     * table
     */
    public AuditLogPath auditLog() {
        if (_auditLog == null)
            _auditLog = new AuditLogPath(this, null, Keys.AUDIT_LOG__AUDIT_LOG_USER_ID_FKEY.getInverseKey());

        return _auditLog;
    }

    private transient CalendarEventPath _calendarEvent;

    /**
     * Get the implicit to-many join path to the
     * <code>public.calendar_event</code> table
     */
    public CalendarEventPath calendarEvent() {
        if (_calendarEvent == null)
            _calendarEvent = new CalendarEventPath(this, null, Keys.CALENDAR_EVENT__CALENDAR_EVENT_OWNER_ID_FKEY.getInverseKey());

        return _calendarEvent;
    }

    private transient CalendarPath _calendar;

    /**
     * Get the implicit to-many join path to the <code>public.calendar</code>
     * table
     */
    public CalendarPath calendar() {
        if (_calendar == null)
            _calendar = new CalendarPath(this, null, Keys.CALENDAR__CALENDAR_OWNER_ID_FKEY.getInverseKey());

        return _calendar;
    }

    private transient ChannelPath _channel;

    /**
     * Get the implicit to-many join path to the <code>public.channel</code>
     * table
     */
    public ChannelPath channel() {
        if (_channel == null)
            _channel = new ChannelPath(this, null, Keys.CHANNEL__CHANNEL_CREATED_BY_FKEY.getInverseKey());

        return _channel;
    }

    private transient ChannelMemberPath _channelMember;

    /**
     * Get the implicit to-many join path to the
     * <code>public.channel_member</code> table
     */
    public ChannelMemberPath channelMember() {
        if (_channelMember == null)
            _channelMember = new ChannelMemberPath(this, null, Keys.CHANNEL_MEMBER__CHANNEL_MEMBER_USER_ID_FKEY.getInverseKey());

        return _channelMember;
    }

    private transient ChatMessageAttachmentPath _chatMessageAttachment;

    /**
     * Get the implicit to-many join path to the
     * <code>public.chat_message_attachment</code> table
     */
    public ChatMessageAttachmentPath chatMessageAttachment() {
        if (_chatMessageAttachment == null)
            _chatMessageAttachment = new ChatMessageAttachmentPath(this, null, Keys.CHAT_MESSAGE_ATTACHMENT__CHAT_MESSAGE_ATTACHMENT_ATTACHED_BY_FKEY.getInverseKey());

        return _chatMessageAttachment;
    }

    private transient ChatMessagePath _chatMessage;

    /**
     * Get the implicit to-many join path to the
     * <code>public.chat_message</code> table
     */
    public ChatMessagePath chatMessage() {
        if (_chatMessage == null)
            _chatMessage = new ChatMessagePath(this, null, Keys.CHAT_MESSAGE__CHAT_MESSAGE_AUTHOR_ID_FKEY.getInverseKey());

        return _chatMessage;
    }

    private transient ChatThreadMemberPath _chatThreadMember;

    /**
     * Get the implicit to-many join path to the
     * <code>public.chat_thread_member</code> table
     */
    public ChatThreadMemberPath chatThreadMember() {
        if (_chatThreadMember == null)
            _chatThreadMember = new ChatThreadMemberPath(this, null, Keys.CHAT_THREAD_MEMBER__CHAT_THREAD_MEMBER_USER_ID_FKEY.getInverseKey());

        return _chatThreadMember;
    }

    private transient ContactEntryPath _contactEntry;

    /**
     * Get the implicit to-many join path to the
     * <code>public.contact_entry</code> table
     */
    public ContactEntryPath contactEntry() {
        if (_contactEntry == null)
            _contactEntry = new ContactEntryPath(this, null, Keys.CONTACT_ENTRY__CONTACT_ENTRY_OWNER_ID_FKEY.getInverseKey());

        return _contactEntry;
    }

    private transient ContactFavoritePath _contactFavorite;

    /**
     * Get the implicit to-many join path to the
     * <code>public.contact_favorite</code> table
     */
    public ContactFavoritePath contactFavorite() {
        if (_contactFavorite == null)
            _contactFavorite = new ContactFavoritePath(this, null, Keys.CONTACT_FAVORITE__CONTACT_FAVORITE_OWNER_ID_FKEY.getInverseKey());

        return _contactFavorite;
    }

    private transient DriveFileRefPath _driveFileRef;

    /**
     * Get the implicit to-many join path to the
     * <code>public.drive_file_ref</code> table
     */
    public DriveFileRefPath driveFileRef() {
        if (_driveFileRef == null)
            _driveFileRef = new DriveFileRefPath(this, null, Keys.DRIVE_FILE_REF__DRIVE_FILE_REF_CREATED_BY_FKEY.getInverseKey());

        return _driveFileRef;
    }

    private transient DriveFileVersionPath _driveFileVersion;

    /**
     * Get the implicit to-many join path to the
     * <code>public.drive_file_version</code> table
     */
    public DriveFileVersionPath driveFileVersion() {
        if (_driveFileVersion == null)
            _driveFileVersion = new DriveFileVersionPath(this, null, Keys.DRIVE_FILE_VERSION__DRIVE_FILE_VERSION_UPLOADED_BY_FKEY.getInverseKey());

        return _driveFileVersion;
    }

    private transient DriveShareLinkPath _driveShareLink;

    /**
     * Get the implicit to-many join path to the
     * <code>public.drive_share_link</code> table
     */
    public DriveShareLinkPath driveShareLink() {
        if (_driveShareLink == null)
            _driveShareLink = new DriveShareLinkPath(this, null, Keys.DRIVE_SHARE_LINK__DRIVE_SHARE_LINK_CREATED_BY_FKEY.getInverseKey());

        return _driveShareLink;
    }

    private transient DriveSpaceMemberPath _driveSpaceMember;

    /**
     * Get the implicit to-many join path to the
     * <code>public.drive_space_member</code> table
     */
    public DriveSpaceMemberPath driveSpaceMember() {
        if (_driveSpaceMember == null)
            _driveSpaceMember = new DriveSpaceMemberPath(this, null, Keys.DRIVE_SPACE_MEMBER__DRIVE_SPACE_MEMBER_USER_ID_FKEY.getInverseKey());

        return _driveSpaceMember;
    }

    private transient DriveSpacePath _driveSpace;

    /**
     * Get the implicit to-many join path to the <code>public.drive_space</code>
     * table
     */
    public DriveSpacePath driveSpace() {
        if (_driveSpace == null)
            _driveSpace = new DriveSpacePath(this, null, Keys.DRIVE_SPACE__DRIVE_SPACE_OWNER_ID_FKEY.getInverseKey());

        return _driveSpace;
    }

    private transient EmailAccountPath _emailAccount;

    /**
     * Get the implicit to-many join path to the
     * <code>public.email_account</code> table
     */
    public EmailAccountPath emailAccount() {
        if (_emailAccount == null)
            _emailAccount = new EmailAccountPath(this, null, Keys.EMAIL_ACCOUNT__EMAIL_ACCOUNT_USER_ID_FKEY.getInverseKey());

        return _emailAccount;
    }

    private transient EventAttendeePath _eventAttendeeInvitedByUserIdFkey;

    /**
     * Get the implicit to-many join path to the
     * <code>public.event_attendee</code> table, via the
     * <code>event_attendee_invited_by_user_id_fkey</code> key
     */
    public EventAttendeePath eventAttendeeInvitedByUserIdFkey() {
        if (_eventAttendeeInvitedByUserIdFkey == null)
            _eventAttendeeInvitedByUserIdFkey = new EventAttendeePath(this, null, Keys.EVENT_ATTENDEE__EVENT_ATTENDEE_INVITED_BY_USER_ID_FKEY.getInverseKey());

        return _eventAttendeeInvitedByUserIdFkey;
    }

    private transient EventAttendeePath _eventAttendeeUserIdFkey;

    /**
     * Get the implicit to-many join path to the
     * <code>public.event_attendee</code> table, via the
     * <code>event_attendee_user_id_fkey</code> key
     */
    public EventAttendeePath eventAttendeeUserIdFkey() {
        if (_eventAttendeeUserIdFkey == null)
            _eventAttendeeUserIdFkey = new EventAttendeePath(this, null, Keys.EVENT_ATTENDEE__EVENT_ATTENDEE_USER_ID_FKEY.getInverseKey());

        return _eventAttendeeUserIdFkey;
    }

    private transient FilePath _file;

    /**
     * Get the implicit to-many join path to the <code>public.file</code> table
     */
    public FilePath file() {
        if (_file == null)
            _file = new FilePath(this, null, Keys.FILE__FILE_UPLOADED_BY_FKEY.getInverseKey());

        return _file;
    }

    private transient HomeSessionPath _homeSession;

    /**
     * Get the implicit to-many join path to the
     * <code>public.home_session</code> table
     */
    public HomeSessionPath homeSession() {
        if (_homeSession == null)
            _homeSession = new HomeSessionPath(this, null, Keys.HOME_SESSION__HOME_SESSION_USER_ID_FKEY.getInverseKey());

        return _homeSession;
    }

    private transient IssueAssigneePath _issueAssigneeAssignedByFkey;

    /**
     * Get the implicit to-many join path to the
     * <code>public.issue_assignee</code> table, via the
     * <code>issue_assignee_assigned_by_fkey</code> key
     */
    public IssueAssigneePath issueAssigneeAssignedByFkey() {
        if (_issueAssigneeAssignedByFkey == null)
            _issueAssigneeAssignedByFkey = new IssueAssigneePath(this, null, Keys.ISSUE_ASSIGNEE__ISSUE_ASSIGNEE_ASSIGNED_BY_FKEY.getInverseKey());

        return _issueAssigneeAssignedByFkey;
    }

    private transient IssueAssigneePath _issueAssigneeUserIdFkey;

    /**
     * Get the implicit to-many join path to the
     * <code>public.issue_assignee</code> table, via the
     * <code>issue_assignee_user_id_fkey</code> key
     */
    public IssueAssigneePath issueAssigneeUserIdFkey() {
        if (_issueAssigneeUserIdFkey == null)
            _issueAssigneeUserIdFkey = new IssueAssigneePath(this, null, Keys.ISSUE_ASSIGNEE__ISSUE_ASSIGNEE_USER_ID_FKEY.getInverseKey());

        return _issueAssigneeUserIdFkey;
    }

    private transient IssueAttachmentPath _issueAttachment;

    /**
     * Get the implicit to-many join path to the
     * <code>public.issue_attachment</code> table
     */
    public IssueAttachmentPath issueAttachment() {
        if (_issueAttachment == null)
            _issueAttachment = new IssueAttachmentPath(this, null, Keys.ISSUE_ATTACHMENT__ISSUE_ATTACHMENT_ATTACHED_BY_FKEY.getInverseKey());

        return _issueAttachment;
    }

    private transient IssueCommentPath _issueComment;

    /**
     * Get the implicit to-many join path to the
     * <code>public.issue_comment</code> table
     */
    public IssueCommentPath issueComment() {
        if (_issueComment == null)
            _issueComment = new IssueCommentPath(this, null, Keys.ISSUE_COMMENT__ISSUE_COMMENT_AUTHOR_ID_FKEY.getInverseKey());

        return _issueComment;
    }

    private transient IssueDependencyPath _issueDependency;

    /**
     * Get the implicit to-many join path to the
     * <code>public.issue_dependency</code> table
     */
    public IssueDependencyPath issueDependency() {
        if (_issueDependency == null)
            _issueDependency = new IssueDependencyPath(this, null, Keys.ISSUE_DEPENDENCY__ISSUE_DEPENDENCY_CREATED_BY_FKEY.getInverseKey());

        return _issueDependency;
    }

    private transient IssueHistoryPath _issueHistory;

    /**
     * Get the implicit to-many join path to the
     * <code>public.issue_history</code> table
     */
    public IssueHistoryPath issueHistory() {
        if (_issueHistory == null)
            _issueHistory = new IssueHistoryPath(this, null, Keys.ISSUE_HISTORY__ISSUE_HISTORY_ACTOR_ID_FKEY.getInverseKey());

        return _issueHistory;
    }

    private transient IssuePath _issue;

    /**
     * Get the implicit to-many join path to the <code>public.issue</code> table
     */
    public IssuePath issue() {
        if (_issue == null)
            _issue = new IssuePath(this, null, Keys.ISSUE__ISSUE_REPORTER_ID_FKEY.getInverseKey());

        return _issue;
    }

    private transient IssueWatcherPath _issueWatcher;

    /**
     * Get the implicit to-many join path to the
     * <code>public.issue_watcher</code> table
     */
    public IssueWatcherPath issueWatcher() {
        if (_issueWatcher == null)
            _issueWatcher = new IssueWatcherPath(this, null, Keys.ISSUE_WATCHER__ISSUE_WATCHER_USER_ID_FKEY.getInverseKey());

        return _issueWatcher;
    }

    private transient MembershipPath _membership;

    /**
     * Get the implicit to-many join path to the <code>public.membership</code>
     * table
     */
    public MembershipPath membership() {
        if (_membership == null)
            _membership = new MembershipPath(this, null, Keys.MEMBERSHIP__MEMBERSHIP_USER_ID_FKEY.getInverseKey());

        return _membership;
    }

    private transient MessageAttachmentPath _messageAttachment;

    /**
     * Get the implicit to-many join path to the
     * <code>public.message_attachment</code> table
     */
    public MessageAttachmentPath messageAttachment() {
        if (_messageAttachment == null)
            _messageAttachment = new MessageAttachmentPath(this, null, Keys.MESSAGE_ATTACHMENT__MESSAGE_ATTACHMENT_ATTACHED_BY_FKEY.getInverseKey());

        return _messageAttachment;
    }

    private transient MessagePath _message;

    /**
     * Get the implicit to-many join path to the <code>public.message</code>
     * table
     */
    public MessagePath message() {
        if (_message == null)
            _message = new MessagePath(this, null, Keys.MESSAGE__MESSAGE_AUTHOR_ID_FKEY.getInverseKey());

        return _message;
    }

    private transient MessageReactionPath _messageReaction;

    /**
     * Get the implicit to-many join path to the
     * <code>public.message_reaction</code> table
     */
    public MessageReactionPath messageReaction() {
        if (_messageReaction == null)
            _messageReaction = new MessageReactionPath(this, null, Keys.MESSAGE_REACTION__MESSAGE_REACTION_USER_ID_FKEY.getInverseKey());

        return _messageReaction;
    }

    private transient NotificationPath _notificationActorIdFkey;

    /**
     * Get the implicit to-many join path to the
     * <code>public.notification</code> table, via the
     * <code>notification_actor_id_fkey</code> key
     */
    public NotificationPath notificationActorIdFkey() {
        if (_notificationActorIdFkey == null)
            _notificationActorIdFkey = new NotificationPath(this, null, Keys.NOTIFICATION__NOTIFICATION_ACTOR_ID_FKEY.getInverseKey());

        return _notificationActorIdFkey;
    }

    private transient NotificationPath _notificationRecipientIdFkey;

    /**
     * Get the implicit to-many join path to the
     * <code>public.notification</code> table, via the
     * <code>notification_recipient_id_fkey</code> key
     */
    public NotificationPath notificationRecipientIdFkey() {
        if (_notificationRecipientIdFkey == null)
            _notificationRecipientIdFkey = new NotificationPath(this, null, Keys.NOTIFICATION__NOTIFICATION_RECIPIENT_ID_FKEY.getInverseKey());

        return _notificationRecipientIdFkey;
    }

    private transient PlatformUserRolePath _platformUserRole;

    /**
     * Get the implicit to-many join path to the
     * <code>public.platform_user_role</code> table
     */
    public PlatformUserRolePath platformUserRole() {
        if (_platformUserRole == null)
            _platformUserRole = new PlatformUserRolePath(this, null, Keys.PLATFORM_USER_ROLE__PLATFORM_USER_ROLE_USER_ID_FKEY.getInverseKey());

        return _platformUserRole;
    }

    private transient ProjectMemberPath _projectMember;

    /**
     * Get the implicit to-many join path to the
     * <code>public.project_member</code> table
     */
    public ProjectMemberPath projectMember() {
        if (_projectMember == null)
            _projectMember = new ProjectMemberPath(this, null, Keys.PROJECT_MEMBER__PROJECT_MEMBER_USER_ID_FKEY.getInverseKey());

        return _projectMember;
    }

    private transient ProjectPath _project;

    /**
     * Get the implicit to-many join path to the <code>public.project</code>
     * table
     */
    public ProjectPath project() {
        if (_project == null)
            _project = new ProjectPath(this, null, Keys.PROJECT__PROJECT_OWNER_ID_FKEY.getInverseKey());

        return _project;
    }

    private transient RefreshTokenPath _refreshToken;

    /**
     * Get the implicit to-many join path to the
     * <code>public.refresh_token</code> table
     */
    public RefreshTokenPath refreshToken() {
        if (_refreshToken == null)
            _refreshToken = new RefreshTokenPath(this, null, Keys.REFRESH_TOKEN__REFRESH_TOKEN_USER_ID_FKEY.getInverseKey());

        return _refreshToken;
    }

    private transient SavedViewPath _savedView;

    /**
     * Get the implicit to-many join path to the <code>public.saved_view</code>
     * table
     */
    public SavedViewPath savedView() {
        if (_savedView == null)
            _savedView = new SavedViewPath(this, null, Keys.SAVED_VIEW__SAVED_VIEW_OWNER_ID_FKEY.getInverseKey());

        return _savedView;
    }

    private transient ThreadReadStatePath _threadReadState;

    /**
     * Get the implicit to-many join path to the
     * <code>public.thread_read_state</code> table
     */
    public ThreadReadStatePath threadReadState() {
        if (_threadReadState == null)
            _threadReadState = new ThreadReadStatePath(this, null, Keys.THREAD_READ_STATE__THREAD_READ_STATE_USER_ID_FKEY.getInverseKey());

        return _threadReadState;
    }

    private transient UserApiTokenPath _userApiToken;

    /**
     * Get the implicit to-many join path to the
     * <code>public.user_api_token</code> table
     */
    public UserApiTokenPath userApiToken() {
        if (_userApiToken == null)
            _userApiToken = new UserApiTokenPath(this, null, Keys.USER_API_TOKEN__USER_API_TOKEN_USER_ID_FKEY.getInverseKey());

        return _userApiToken;
    }

    private transient UserDashboardPath _userDashboard;

    /**
     * Get the implicit to-many join path to the
     * <code>public.user_dashboard</code> table
     */
    public UserDashboardPath userDashboard() {
        if (_userDashboard == null)
            _userDashboard = new UserDashboardPath(this, null, Keys.USER_DASHBOARD__USER_DASHBOARD_USER_ID_FKEY.getInverseKey());

        return _userDashboard;
    }

    private transient UserGroupPath _userGroup;

    /**
     * Get the implicit to-many join path to the <code>public.user_group</code>
     * table
     */
    public UserGroupPath userGroup() {
        if (_userGroup == null)
            _userGroup = new UserGroupPath(this, null, Keys.USER_GROUP__USER_GROUP_OWNER_ID_FKEY.getInverseKey());

        return _userGroup;
    }

    private transient UserPriorityItemPath _userPriorityItem;

    /**
     * Get the implicit to-many join path to the
     * <code>public.user_priority_item</code> table
     */
    public UserPriorityItemPath userPriorityItem() {
        if (_userPriorityItem == null)
            _userPriorityItem = new UserPriorityItemPath(this, null, Keys.USER_PRIORITY_ITEM__USER_PRIORITY_ITEM_USER_ID_FKEY.getInverseKey());

        return _userPriorityItem;
    }

    private transient UserRolePath _userRole;

    /**
     * Get the implicit to-many join path to the <code>public.user_role</code>
     * table
     */
    public UserRolePath userRole() {
        if (_userRole == null)
            _userRole = new UserRolePath(this, null, Keys.USER_ROLE__USER_ROLE_USER_ID_FKEY.getInverseKey());

        return _userRole;
    }

    private transient WikiSpaceMemberPath _wikiSpaceMember;

    /**
     * Get the implicit to-many join path to the
     * <code>public.wiki_space_member</code> table
     */
    public WikiSpaceMemberPath wikiSpaceMember() {
        if (_wikiSpaceMember == null)
            _wikiSpaceMember = new WikiSpaceMemberPath(this, null, Keys.WIKI_SPACE_MEMBER__WIKI_SPACE_MEMBER_USER_ID_FKEY.getInverseKey());

        return _wikiSpaceMember;
    }

    private transient WikiSpacePath _wikiSpace;

    /**
     * Get the implicit to-many join path to the <code>public.wiki_space</code>
     * table
     */
    public WikiSpacePath wikiSpace() {
        if (_wikiSpace == null)
            _wikiSpace = new WikiSpacePath(this, null, Keys.WIKI_SPACE__WIKI_SPACE_OWNER_ID_FKEY.getInverseKey());

        return _wikiSpace;
    }

    private transient WorkspaceAssistantPath _workspaceAssistantAgentUserIdFkey;

    /**
     * Get the implicit to-many join path to the
     * <code>public.workspace_assistant</code> table, via the
     * <code>workspace_assistant_agent_user_id_fkey</code> key
     */
    public WorkspaceAssistantPath workspaceAssistantAgentUserIdFkey() {
        if (_workspaceAssistantAgentUserIdFkey == null)
            _workspaceAssistantAgentUserIdFkey = new WorkspaceAssistantPath(this, null, Keys.WORKSPACE_ASSISTANT__WORKSPACE_ASSISTANT_AGENT_USER_ID_FKEY.getInverseKey());

        return _workspaceAssistantAgentUserIdFkey;
    }

    private transient WorkspaceAssistantPath _workspaceAssistantUpdatedByFkey;

    /**
     * Get the implicit to-many join path to the
     * <code>public.workspace_assistant</code> table, via the
     * <code>workspace_assistant_updated_by_fkey</code> key
     */
    public WorkspaceAssistantPath workspaceAssistantUpdatedByFkey() {
        if (_workspaceAssistantUpdatedByFkey == null)
            _workspaceAssistantUpdatedByFkey = new WorkspaceAssistantPath(this, null, Keys.WORKSPACE_ASSISTANT__WORKSPACE_ASSISTANT_UPDATED_BY_FKEY.getInverseKey());

        return _workspaceAssistantUpdatedByFkey;
    }

    /**
     * Get the implicit many-to-many join path to the
     * <code>public.chat_thread</code> table
     */
    public ChatThreadPath chatThread() {
        return chatThreadMember().chatThread();
    }

    /**
     * Get the implicit many-to-many join path to the <code>public.issue</code>
     * table, via the <code>issue_assignee_issue_id_fkey</code> key
     */
    public IssuePath issueAssigneeIssueIdFkey() {
        return issueAssigneeUserIdFkey().issue();
    }

    /**
     * Get the implicit many-to-many join path to the <code>public.issue</code>
     * table, via the <code>issue_watcher_issue_id_fkey</code> key
     */
    public IssuePath issueWatcherIssueIdFkey() {
        return issueWatcher().issue();
    }

    /**
     * Get the implicit many-to-many join path to the <code>public.tenant</code>
     * table, via the <code>membership_tenant_id_fkey</code> key
     */
    public TenantPath membershipTenantIdFkey() {
        return membership().tenant();
    }

    /**
     * Get the implicit many-to-many join path to the
     * <code>public.platform_role</code> table
     */
    public PlatformRolePath platformRole() {
        return platformUserRole().platformRole();
    }

    /**
     * Get the implicit many-to-many join path to the <code>public.tenant</code>
     * table, via the <code>user_dashboard_tenant_id_fkey</code> key
     */
    public TenantPath userDashboardTenantIdFkey() {
        return userDashboard().tenant();
    }

    /**
     * Get the implicit many-to-many join path to the <code>public.role</code>
     * table
     */
    public RolePath role() {
        return userRole().role();
    }

    @Override
    public List<Check<UserRecord>> getChecks() {
        return Arrays.asList(
            Internal.createCheck(this, DSL.name("user_kind_check"), "(((kind)::text = ANY ((ARRAY['HUMAN'::character varying, 'AGENT'::character varying])::text[])))", true)
        );
    }

    @Override
    public User as(String alias) {
        return new User(DSL.name(alias), this);
    }

    @Override
    public User as(Name alias) {
        return new User(alias, this);
    }

    @Override
    public User as(Table<?> alias) {
        return new User(alias.getQualifiedName(), this);
    }

    /**
     * Rename this table
     */
    @Override
    public User rename(String name) {
        return new User(DSL.name(name), null);
    }

    /**
     * Rename this table
     */
    @Override
    public User rename(Name name) {
        return new User(name, null);
    }

    /**
     * Rename this table
     */
    @Override
    public User rename(Table<?> name) {
        return new User(name.getQualifiedName(), null);
    }

    /**
     * Create an inline derived table from this table
     */
    @Override
    public User where(Condition condition) {
        return new User(getQualifiedName(), aliased() ? this : null, null, condition);
    }

    /**
     * Create an inline derived table from this table
     */
    @Override
    public User where(Collection<? extends Condition> conditions) {
        return where(DSL.and(conditions));
    }

    /**
     * Create an inline derived table from this table
     */
    @Override
    public User where(Condition... conditions) {
        return where(DSL.and(conditions));
    }

    /**
     * Create an inline derived table from this table
     */
    @Override
    public User where(Field<Boolean> condition) {
        return where(DSL.condition(condition));
    }

    /**
     * Create an inline derived table from this table
     */
    @Override
    @PlainSQL
    public User where(SQL condition) {
        return where(DSL.condition(condition));
    }

    /**
     * Create an inline derived table from this table
     */
    @Override
    @PlainSQL
    public User where(@Stringly.SQL String condition) {
        return where(DSL.condition(condition));
    }

    /**
     * Create an inline derived table from this table
     */
    @Override
    @PlainSQL
    public User where(@Stringly.SQL String condition, Object... binds) {
        return where(DSL.condition(condition, binds));
    }

    /**
     * Create an inline derived table from this table
     */
    @Override
    @PlainSQL
    public User where(@Stringly.SQL String condition, QueryPart... parts) {
        return where(DSL.condition(condition, parts));
    }

    /**
     * Create an inline derived table from this table
     */
    @Override
    public User whereExists(Select<?> select) {
        return where(DSL.exists(select));
    }

    /**
     * Create an inline derived table from this table
     */
    @Override
    public User whereNotExists(Select<?> select) {
        return where(DSL.notExists(select));
    }
}
