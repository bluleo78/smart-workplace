/*
 * This file is generated by jOOQ.
 */
package com.workplace.jooq.tables;


import com.workplace.jooq.Keys;
import com.workplace.jooq.Public;
import com.workplace.jooq.tables.AssistantConfig.AssistantConfigPath;
import com.workplace.jooq.tables.AuditLog.AuditLogPath;
import com.workplace.jooq.tables.Calendar.CalendarPath;
import com.workplace.jooq.tables.CalendarEvent.CalendarEventPath;
import com.workplace.jooq.tables.CalendarEventException.CalendarEventExceptionPath;
import com.workplace.jooq.tables.Channel.ChannelPath;
import com.workplace.jooq.tables.ChannelMember.ChannelMemberPath;
import com.workplace.jooq.tables.ChatMessage.ChatMessagePath;
import com.workplace.jooq.tables.ChatMessageAttachment.ChatMessageAttachmentPath;
import com.workplace.jooq.tables.ChatThread.ChatThreadPath;
import com.workplace.jooq.tables.ChatThreadMember.ChatThreadMemberPath;
import com.workplace.jooq.tables.ContactEntry.ContactEntryPath;
import com.workplace.jooq.tables.ContactFavorite.ContactFavoritePath;
import com.workplace.jooq.tables.Cycle.CyclePath;
import com.workplace.jooq.tables.DriveFile.DriveFilePath;
import com.workplace.jooq.tables.DriveFileRef.DriveFileRefPath;
import com.workplace.jooq.tables.DriveFileVersion.DriveFileVersionPath;
import com.workplace.jooq.tables.DriveFolder.DriveFolderPath;
import com.workplace.jooq.tables.DriveShareLink.DriveShareLinkPath;
import com.workplace.jooq.tables.DriveSpace.DriveSpacePath;
import com.workplace.jooq.tables.DriveSpaceMember.DriveSpaceMemberPath;
import com.workplace.jooq.tables.EmailAccount.EmailAccountPath;
import com.workplace.jooq.tables.EmailAttachment.EmailAttachmentPath;
import com.workplace.jooq.tables.EmailContent.EmailContentPath;
import com.workplace.jooq.tables.EmailFolder.EmailFolderPath;
import com.workplace.jooq.tables.EmailMessage.EmailMessagePath;
import com.workplace.jooq.tables.EventAttendee.EventAttendeePath;
import com.workplace.jooq.tables.EventReminder.EventReminderPath;
import com.workplace.jooq.tables.File.FilePath;
import com.workplace.jooq.tables.FileExtraction.FileExtractionPath;
import com.workplace.jooq.tables.HomeMessage.HomeMessagePath;
import com.workplace.jooq.tables.HomeSession.HomeSessionPath;
import com.workplace.jooq.tables.Issue.IssuePath;
import com.workplace.jooq.tables.IssueAiSummary.IssueAiSummaryPath;
import com.workplace.jooq.tables.IssueAssignee.IssueAssigneePath;
import com.workplace.jooq.tables.IssueAttachment.IssueAttachmentPath;
import com.workplace.jooq.tables.IssueComment.IssueCommentPath;
import com.workplace.jooq.tables.IssueCycle.IssueCyclePath;
import com.workplace.jooq.tables.IssueDependency.IssueDependencyPath;
import com.workplace.jooq.tables.IssueFieldDef.IssueFieldDefPath;
import com.workplace.jooq.tables.IssueFieldValue.IssueFieldValuePath;
import com.workplace.jooq.tables.IssueHistory.IssueHistoryPath;
import com.workplace.jooq.tables.IssueLabel.IssueLabelPath;
import com.workplace.jooq.tables.IssueTypeDef.IssueTypeDefPath;
import com.workplace.jooq.tables.IssueWatcher.IssueWatcherPath;
import com.workplace.jooq.tables.Label.LabelPath;
import com.workplace.jooq.tables.Membership.MembershipPath;
import com.workplace.jooq.tables.Message.MessagePath;
import com.workplace.jooq.tables.MessageAttachment.MessageAttachmentPath;
import com.workplace.jooq.tables.MessageReaction.MessageReactionPath;
import com.workplace.jooq.tables.Milestone.MilestonePath;
import com.workplace.jooq.tables.Notification.NotificationPath;
import com.workplace.jooq.tables.Project.ProjectPath;
import com.workplace.jooq.tables.ProjectIssueSequence.ProjectIssueSequencePath;
import com.workplace.jooq.tables.ProjectMember.ProjectMemberPath;
import com.workplace.jooq.tables.Role.RolePath;
import com.workplace.jooq.tables.RolePermission.RolePermissionPath;
import com.workplace.jooq.tables.SavedView.SavedViewPath;
import com.workplace.jooq.tables.ThreadReadState.ThreadReadStatePath;
import com.workplace.jooq.tables.User.UserPath;
import com.workplace.jooq.tables.UserApiToken.UserApiTokenPath;
import com.workplace.jooq.tables.UserDashboard.UserDashboardPath;
import com.workplace.jooq.tables.UserGroup.UserGroupPath;
import com.workplace.jooq.tables.UserGroupMember.UserGroupMemberPath;
import com.workplace.jooq.tables.UserPriorityItem.UserPriorityItemPath;
import com.workplace.jooq.tables.UserRole.UserRolePath;
import com.workplace.jooq.tables.WikiPage.WikiPagePath;
import com.workplace.jooq.tables.WikiPageAttachment.WikiPageAttachmentPath;
import com.workplace.jooq.tables.WikiReference.WikiReferencePath;
import com.workplace.jooq.tables.WikiRevision.WikiRevisionPath;
import com.workplace.jooq.tables.WikiSpace.WikiSpacePath;
import com.workplace.jooq.tables.WikiSpaceMember.WikiSpaceMemberPath;
import com.workplace.jooq.tables.WorkerJob.WorkerJobPath;
import com.workplace.jooq.tables.WorkspaceAssistant.WorkspaceAssistantPath;
import com.workplace.jooq.tables.records.TenantRecord;

import java.time.LocalDateTime;
import java.util.Arrays;
import java.util.Collection;
import java.util.List;

import org.jooq.Check;
import org.jooq.Condition;
import org.jooq.Field;
import org.jooq.ForeignKey;
import org.jooq.Identity;
import org.jooq.InverseForeignKey;
import org.jooq.Name;
import org.jooq.Path;
import org.jooq.PlainSQL;
import org.jooq.QueryPart;
import org.jooq.Record;
import org.jooq.SQL;
import org.jooq.Schema;
import org.jooq.Select;
import org.jooq.Stringly;
import org.jooq.Table;
import org.jooq.TableField;
import org.jooq.TableOptions;
import org.jooq.UniqueKey;
import org.jooq.impl.DSL;
import org.jooq.impl.Internal;
import org.jooq.impl.SQLDataType;
import org.jooq.impl.TableImpl;


/**
 * 테넌트(고객사 워크스페이스) — 글로벌
 */
@SuppressWarnings({ "all", "unchecked", "rawtypes", "this-escape" })
public class Tenant extends TableImpl<TenantRecord> {

    private static final long serialVersionUID = 1L;

    /**
     * The reference instance of <code>public.tenant</code>
     */
    public static final Tenant TENANT = new Tenant();

    /**
     * The class holding records for this type
     */
    @Override
    public Class<TenantRecord> getRecordType() {
        return TenantRecord.class;
    }

    /**
     * The column <code>public.tenant.id</code>.
     */
    public final TableField<TenantRecord, Long> ID = createField(DSL.name("id"), SQLDataType.BIGINT.nullable(false).identity(true), this, "");

    /**
     * The column <code>public.tenant.slug</code>.
     */
    public final TableField<TenantRecord, String> SLUG = createField(DSL.name("slug"), SQLDataType.VARCHAR(64), this, "");

    /**
     * The column <code>public.tenant.name</code>.
     */
    public final TableField<TenantRecord, String> NAME = createField(DSL.name("name"), SQLDataType.VARCHAR(255).nullable(false), this, "");

    /**
     * The column <code>public.tenant.status</code>.
     */
    public final TableField<TenantRecord, String> STATUS = createField(DSL.name("status"), SQLDataType.VARCHAR(16).nullable(false).defaultValue(DSL.field(DSL.raw("'ACTIVE'::character varying"), SQLDataType.VARCHAR)), this, "");

    /**
     * The column <code>public.tenant.created_at</code>.
     */
    public final TableField<TenantRecord, LocalDateTime> CREATED_AT = createField(DSL.name("created_at"), SQLDataType.LOCALDATETIME(6).nullable(false).defaultValue(DSL.field(DSL.raw("now()"), SQLDataType.LOCALDATETIME)), this, "");

    /**
     * The column <code>public.tenant.quota_bytes</code>. 드라이브 저장 한도(바이트). 기본
     * 10GB.
     */
    public final TableField<TenantRecord, Long> QUOTA_BYTES = createField(DSL.name("quota_bytes"), SQLDataType.BIGINT.nullable(false).defaultValue(DSL.field(DSL.raw("'10737418240'::bigint"), SQLDataType.BIGINT)), this, "드라이브 저장 한도(바이트). 기본 10GB.");

    private Tenant(Name alias, Table<TenantRecord> aliased) {
        this(alias, aliased, (Field<?>[]) null, null);
    }

    private Tenant(Name alias, Table<TenantRecord> aliased, Field<?>[] parameters, Condition where) {
        super(alias, null, aliased, parameters, DSL.comment("테넌트(고객사 워크스페이스) — 글로벌"), TableOptions.table(), where);
    }

    /**
     * Create an aliased <code>public.tenant</code> table reference
     */
    public Tenant(String alias) {
        this(DSL.name(alias), TENANT);
    }

    /**
     * Create an aliased <code>public.tenant</code> table reference
     */
    public Tenant(Name alias) {
        this(alias, TENANT);
    }

    /**
     * Create a <code>public.tenant</code> table reference
     */
    public Tenant() {
        this(DSL.name("tenant"), null);
    }

    public <O extends Record> Tenant(Table<O> path, ForeignKey<O, TenantRecord> childPath, InverseForeignKey<O, TenantRecord> parentPath) {
        super(path, childPath, parentPath, TENANT);
    }

    /**
     * A subtype implementing {@link Path} for simplified path-based joins.
     */
    public static class TenantPath extends Tenant implements Path<TenantRecord> {

        private static final long serialVersionUID = 1L;
        public <O extends Record> TenantPath(Table<O> path, ForeignKey<O, TenantRecord> childPath, InverseForeignKey<O, TenantRecord> parentPath) {
            super(path, childPath, parentPath);
        }
        private TenantPath(Name alias, Table<TenantRecord> aliased) {
            super(alias, aliased);
        }

        @Override
        public TenantPath as(String alias) {
            return new TenantPath(DSL.name(alias), this);
        }

        @Override
        public TenantPath as(Name alias) {
            return new TenantPath(alias, this);
        }

        @Override
        public TenantPath as(Table<?> alias) {
            return new TenantPath(alias.getQualifiedName(), this);
        }
    }

    @Override
    public Schema getSchema() {
        return aliased() ? null : Public.PUBLIC;
    }

    @Override
    public Identity<TenantRecord, Long> getIdentity() {
        return (Identity<TenantRecord, Long>) super.getIdentity();
    }

    @Override
    public UniqueKey<TenantRecord> getPrimaryKey() {
        return Keys.TENANT_PKEY;
    }

    @Override
    public List<UniqueKey<TenantRecord>> getUniqueKeys() {
        return Arrays.asList(Keys.TENANT_SLUG_KEY);
    }

    private transient ChatMessageAttachmentPath _chatMessageAttachment;

    /**
     * Get the implicit to-many join path to the
     * <code>public.chat_message_attachment</code> table
     */
    public ChatMessageAttachmentPath chatMessageAttachment() {
        if (_chatMessageAttachment == null)
            _chatMessageAttachment = new ChatMessageAttachmentPath(this, null, Keys.CHAT_MESSAGE_ATTACHMENT__CHAT_MESSAGE_ATTACHMENT_TENANT_ID_FKEY.getInverseKey());

        return _chatMessageAttachment;
    }

    private transient DriveFileRefPath _driveFileRef;

    /**
     * Get the implicit to-many join path to the
     * <code>public.drive_file_ref</code> table
     */
    public DriveFileRefPath driveFileRef() {
        if (_driveFileRef == null)
            _driveFileRef = new DriveFileRefPath(this, null, Keys.DRIVE_FILE_REF__DRIVE_FILE_REF_TENANT_ID_FKEY.getInverseKey());

        return _driveFileRef;
    }

    private transient DriveFileVersionPath _driveFileVersion;

    /**
     * Get the implicit to-many join path to the
     * <code>public.drive_file_version</code> table
     */
    public DriveFileVersionPath driveFileVersion() {
        if (_driveFileVersion == null)
            _driveFileVersion = new DriveFileVersionPath(this, null, Keys.DRIVE_FILE_VERSION__DRIVE_FILE_VERSION_TENANT_ID_FKEY.getInverseKey());

        return _driveFileVersion;
    }

    private transient DriveShareLinkPath _driveShareLink;

    /**
     * Get the implicit to-many join path to the
     * <code>public.drive_share_link</code> table
     */
    public DriveShareLinkPath driveShareLink() {
        if (_driveShareLink == null)
            _driveShareLink = new DriveShareLinkPath(this, null, Keys.DRIVE_SHARE_LINK__DRIVE_SHARE_LINK_TENANT_ID_FKEY.getInverseKey());

        return _driveShareLink;
    }

    private transient EmailContentPath _emailContent;

    /**
     * Get the implicit to-many join path to the
     * <code>public.email_content</code> table
     */
    public EmailContentPath emailContent() {
        if (_emailContent == null)
            _emailContent = new EmailContentPath(this, null, Keys.EMAIL_CONTENT__EMAIL_CONTENT_TENANT_ID_FKEY.getInverseKey());

        return _emailContent;
    }

    private transient FileExtractionPath _fileExtraction;

    /**
     * Get the implicit to-many join path to the
     * <code>public.file_extraction</code> table
     */
    public FileExtractionPath fileExtraction() {
        if (_fileExtraction == null)
            _fileExtraction = new FileExtractionPath(this, null, Keys.FILE_EXTRACTION__FILE_EXTRACTION_TENANT_ID_FKEY.getInverseKey());

        return _fileExtraction;
    }

    private transient AssistantConfigPath _assistantConfig;

    /**
     * Get the implicit to-many join path to the
     * <code>public.assistant_config</code> table
     */
    public AssistantConfigPath assistantConfig() {
        if (_assistantConfig == null)
            _assistantConfig = new AssistantConfigPath(this, null, Keys.ASSISTANT_CONFIG__FK_ASSISTANT_CONFIG_TENANT.getInverseKey());

        return _assistantConfig;
    }

    private transient AuditLogPath _auditLog;

    /**
     * Get the implicit to-many join path to the <code>public.audit_log</code>
     * table
     */
    public AuditLogPath auditLog() {
        if (_auditLog == null)
            _auditLog = new AuditLogPath(this, null, Keys.AUDIT_LOG__FK_AUDIT_LOG_TENANT.getInverseKey());

        return _auditLog;
    }

    private transient CalendarEventExceptionPath _calendarEventException;

    /**
     * Get the implicit to-many join path to the
     * <code>public.calendar_event_exception</code> table
     */
    public CalendarEventExceptionPath calendarEventException() {
        if (_calendarEventException == null)
            _calendarEventException = new CalendarEventExceptionPath(this, null, Keys.CALENDAR_EVENT_EXCEPTION__FK_CALENDAR_EVENT_EXCEPTION_TENANT.getInverseKey());

        return _calendarEventException;
    }

    private transient CalendarEventPath _calendarEvent;

    /**
     * Get the implicit to-many join path to the
     * <code>public.calendar_event</code> table
     */
    public CalendarEventPath calendarEvent() {
        if (_calendarEvent == null)
            _calendarEvent = new CalendarEventPath(this, null, Keys.CALENDAR_EVENT__FK_CALENDAR_EVENT_TENANT.getInverseKey());

        return _calendarEvent;
    }

    private transient CalendarPath _calendar;

    /**
     * Get the implicit to-many join path to the <code>public.calendar</code>
     * table
     */
    public CalendarPath calendar() {
        if (_calendar == null)
            _calendar = new CalendarPath(this, null, Keys.CALENDAR__FK_CALENDAR_TENANT.getInverseKey());

        return _calendar;
    }

    private transient ChannelMemberPath _channelMember;

    /**
     * Get the implicit to-many join path to the
     * <code>public.channel_member</code> table
     */
    public ChannelMemberPath channelMember() {
        if (_channelMember == null)
            _channelMember = new ChannelMemberPath(this, null, Keys.CHANNEL_MEMBER__FK_CHANNEL_MEMBER_TENANT.getInverseKey());

        return _channelMember;
    }

    private transient ChannelPath _channel;

    /**
     * Get the implicit to-many join path to the <code>public.channel</code>
     * table
     */
    public ChannelPath channel() {
        if (_channel == null)
            _channel = new ChannelPath(this, null, Keys.CHANNEL__FK_CHANNEL_TENANT.getInverseKey());

        return _channel;
    }

    private transient ChatMessagePath _chatMessage;

    /**
     * Get the implicit to-many join path to the
     * <code>public.chat_message</code> table
     */
    public ChatMessagePath chatMessage() {
        if (_chatMessage == null)
            _chatMessage = new ChatMessagePath(this, null, Keys.CHAT_MESSAGE__FK_CHAT_MESSAGE_TENANT.getInverseKey());

        return _chatMessage;
    }

    private transient ChatThreadMemberPath _chatThreadMember;

    /**
     * Get the implicit to-many join path to the
     * <code>public.chat_thread_member</code> table
     */
    public ChatThreadMemberPath chatThreadMember() {
        if (_chatThreadMember == null)
            _chatThreadMember = new ChatThreadMemberPath(this, null, Keys.CHAT_THREAD_MEMBER__FK_CHAT_THREAD_MEMBER_TENANT.getInverseKey());

        return _chatThreadMember;
    }

    private transient ChatThreadPath _chatThread;

    /**
     * Get the implicit to-many join path to the <code>public.chat_thread</code>
     * table
     */
    public ChatThreadPath chatThread() {
        if (_chatThread == null)
            _chatThread = new ChatThreadPath(this, null, Keys.CHAT_THREAD__FK_CHAT_THREAD_TENANT.getInverseKey());

        return _chatThread;
    }

    private transient ContactEntryPath _contactEntry;

    /**
     * Get the implicit to-many join path to the
     * <code>public.contact_entry</code> table
     */
    public ContactEntryPath contactEntry() {
        if (_contactEntry == null)
            _contactEntry = new ContactEntryPath(this, null, Keys.CONTACT_ENTRY__FK_CONTACT_ENTRY_TENANT.getInverseKey());

        return _contactEntry;
    }

    private transient ContactFavoritePath _contactFavorite;

    /**
     * Get the implicit to-many join path to the
     * <code>public.contact_favorite</code> table
     */
    public ContactFavoritePath contactFavorite() {
        if (_contactFavorite == null)
            _contactFavorite = new ContactFavoritePath(this, null, Keys.CONTACT_FAVORITE__FK_CONTACT_FAVORITE_TENANT.getInverseKey());

        return _contactFavorite;
    }

    private transient CyclePath _cycle;

    /**
     * Get the implicit to-many join path to the <code>public.cycle</code> table
     */
    public CyclePath cycle() {
        if (_cycle == null)
            _cycle = new CyclePath(this, null, Keys.CYCLE__FK_CYCLE_TENANT.getInverseKey());

        return _cycle;
    }

    private transient DriveFilePath _driveFile;

    /**
     * Get the implicit to-many join path to the <code>public.drive_file</code>
     * table
     */
    public DriveFilePath driveFile() {
        if (_driveFile == null)
            _driveFile = new DriveFilePath(this, null, Keys.DRIVE_FILE__FK_DRIVE_FILE_TENANT.getInverseKey());

        return _driveFile;
    }

    private transient DriveFolderPath _driveFolder;

    /**
     * Get the implicit to-many join path to the
     * <code>public.drive_folder</code> table
     */
    public DriveFolderPath driveFolder() {
        if (_driveFolder == null)
            _driveFolder = new DriveFolderPath(this, null, Keys.DRIVE_FOLDER__FK_DRIVE_FOLDER_TENANT.getInverseKey());

        return _driveFolder;
    }

    private transient DriveSpaceMemberPath _driveSpaceMember;

    /**
     * Get the implicit to-many join path to the
     * <code>public.drive_space_member</code> table
     */
    public DriveSpaceMemberPath driveSpaceMember() {
        if (_driveSpaceMember == null)
            _driveSpaceMember = new DriveSpaceMemberPath(this, null, Keys.DRIVE_SPACE_MEMBER__FK_DRIVE_SPACE_MEMBER_TENANT.getInverseKey());

        return _driveSpaceMember;
    }

    private transient DriveSpacePath _driveSpace;

    /**
     * Get the implicit to-many join path to the <code>public.drive_space</code>
     * table
     */
    public DriveSpacePath driveSpace() {
        if (_driveSpace == null)
            _driveSpace = new DriveSpacePath(this, null, Keys.DRIVE_SPACE__FK_DRIVE_SPACE_TENANT.getInverseKey());

        return _driveSpace;
    }

    private transient EmailAccountPath _emailAccount;

    /**
     * Get the implicit to-many join path to the
     * <code>public.email_account</code> table
     */
    public EmailAccountPath emailAccount() {
        if (_emailAccount == null)
            _emailAccount = new EmailAccountPath(this, null, Keys.EMAIL_ACCOUNT__FK_EMAIL_ACCOUNT_TENANT.getInverseKey());

        return _emailAccount;
    }

    private transient EmailAttachmentPath _emailAttachment;

    /**
     * Get the implicit to-many join path to the
     * <code>public.email_attachment</code> table
     */
    public EmailAttachmentPath emailAttachment() {
        if (_emailAttachment == null)
            _emailAttachment = new EmailAttachmentPath(this, null, Keys.EMAIL_ATTACHMENT__FK_EMAIL_ATTACHMENT_TENANT.getInverseKey());

        return _emailAttachment;
    }

    private transient EmailFolderPath _emailFolder;

    /**
     * Get the implicit to-many join path to the
     * <code>public.email_folder</code> table
     */
    public EmailFolderPath emailFolder() {
        if (_emailFolder == null)
            _emailFolder = new EmailFolderPath(this, null, Keys.EMAIL_FOLDER__FK_EMAIL_FOLDER_TENANT.getInverseKey());

        return _emailFolder;
    }

    private transient EmailMessagePath _emailMessage;

    /**
     * Get the implicit to-many join path to the
     * <code>public.email_message</code> table
     */
    public EmailMessagePath emailMessage() {
        if (_emailMessage == null)
            _emailMessage = new EmailMessagePath(this, null, Keys.EMAIL_MESSAGE__FK_EMAIL_MESSAGE_TENANT.getInverseKey());

        return _emailMessage;
    }

    private transient EventAttendeePath _eventAttendee;

    /**
     * Get the implicit to-many join path to the
     * <code>public.event_attendee</code> table
     */
    public EventAttendeePath eventAttendee() {
        if (_eventAttendee == null)
            _eventAttendee = new EventAttendeePath(this, null, Keys.EVENT_ATTENDEE__FK_EVENT_ATTENDEE_TENANT.getInverseKey());

        return _eventAttendee;
    }

    private transient EventReminderPath _eventReminder;

    /**
     * Get the implicit to-many join path to the
     * <code>public.event_reminder</code> table
     */
    public EventReminderPath eventReminder() {
        if (_eventReminder == null)
            _eventReminder = new EventReminderPath(this, null, Keys.EVENT_REMINDER__FK_EVENT_REMINDER_TENANT.getInverseKey());

        return _eventReminder;
    }

    private transient FilePath _file;

    /**
     * Get the implicit to-many join path to the <code>public.file</code> table
     */
    public FilePath file() {
        if (_file == null)
            _file = new FilePath(this, null, Keys.FILE__FK_FILE_TENANT.getInverseKey());

        return _file;
    }

    private transient HomeMessagePath _homeMessage;

    /**
     * Get the implicit to-many join path to the
     * <code>public.home_message</code> table
     */
    public HomeMessagePath homeMessage() {
        if (_homeMessage == null)
            _homeMessage = new HomeMessagePath(this, null, Keys.HOME_MESSAGE__FK_HOME_MESSAGE_TENANT.getInverseKey());

        return _homeMessage;
    }

    private transient HomeSessionPath _homeSession;

    /**
     * Get the implicit to-many join path to the
     * <code>public.home_session</code> table
     */
    public HomeSessionPath homeSession() {
        if (_homeSession == null)
            _homeSession = new HomeSessionPath(this, null, Keys.HOME_SESSION__FK_HOME_SESSION_TENANT.getInverseKey());

        return _homeSession;
    }

    private transient IssueAiSummaryPath _issueAiSummary;

    /**
     * Get the implicit to-many join path to the
     * <code>public.issue_ai_summary</code> table
     */
    public IssueAiSummaryPath issueAiSummary() {
        if (_issueAiSummary == null)
            _issueAiSummary = new IssueAiSummaryPath(this, null, Keys.ISSUE_AI_SUMMARY__FK_ISSUE_AI_SUMMARY_TENANT.getInverseKey());

        return _issueAiSummary;
    }

    private transient IssueAssigneePath _issueAssignee;

    /**
     * Get the implicit to-many join path to the
     * <code>public.issue_assignee</code> table
     */
    public IssueAssigneePath issueAssignee() {
        if (_issueAssignee == null)
            _issueAssignee = new IssueAssigneePath(this, null, Keys.ISSUE_ASSIGNEE__FK_ISSUE_ASSIGNEE_TENANT.getInverseKey());

        return _issueAssignee;
    }

    private transient IssueAttachmentPath _issueAttachment;

    /**
     * Get the implicit to-many join path to the
     * <code>public.issue_attachment</code> table
     */
    public IssueAttachmentPath issueAttachment() {
        if (_issueAttachment == null)
            _issueAttachment = new IssueAttachmentPath(this, null, Keys.ISSUE_ATTACHMENT__FK_ISSUE_ATTACHMENT_TENANT.getInverseKey());

        return _issueAttachment;
    }

    private transient IssueCommentPath _issueComment;

    /**
     * Get the implicit to-many join path to the
     * <code>public.issue_comment</code> table
     */
    public IssueCommentPath issueComment() {
        if (_issueComment == null)
            _issueComment = new IssueCommentPath(this, null, Keys.ISSUE_COMMENT__FK_ISSUE_COMMENT_TENANT.getInverseKey());

        return _issueComment;
    }

    private transient IssueCyclePath _issueCycle;

    /**
     * Get the implicit to-many join path to the <code>public.issue_cycle</code>
     * table
     */
    public IssueCyclePath issueCycle() {
        if (_issueCycle == null)
            _issueCycle = new IssueCyclePath(this, null, Keys.ISSUE_CYCLE__FK_ISSUE_CYCLE_TENANT.getInverseKey());

        return _issueCycle;
    }

    private transient IssueDependencyPath _issueDependency;

    /**
     * Get the implicit to-many join path to the
     * <code>public.issue_dependency</code> table
     */
    public IssueDependencyPath issueDependency() {
        if (_issueDependency == null)
            _issueDependency = new IssueDependencyPath(this, null, Keys.ISSUE_DEPENDENCY__FK_ISSUE_DEPENDENCY_TENANT.getInverseKey());

        return _issueDependency;
    }

    private transient IssueFieldDefPath _issueFieldDef;

    /**
     * Get the implicit to-many join path to the
     * <code>public.issue_field_def</code> table
     */
    public IssueFieldDefPath issueFieldDef() {
        if (_issueFieldDef == null)
            _issueFieldDef = new IssueFieldDefPath(this, null, Keys.ISSUE_FIELD_DEF__FK_ISSUE_FIELD_DEF_TENANT.getInverseKey());

        return _issueFieldDef;
    }

    private transient IssueFieldValuePath _issueFieldValue;

    /**
     * Get the implicit to-many join path to the
     * <code>public.issue_field_value</code> table
     */
    public IssueFieldValuePath issueFieldValue() {
        if (_issueFieldValue == null)
            _issueFieldValue = new IssueFieldValuePath(this, null, Keys.ISSUE_FIELD_VALUE__FK_ISSUE_FIELD_VALUE_TENANT.getInverseKey());

        return _issueFieldValue;
    }

    private transient IssueHistoryPath _issueHistory;

    /**
     * Get the implicit to-many join path to the
     * <code>public.issue_history</code> table
     */
    public IssueHistoryPath issueHistory() {
        if (_issueHistory == null)
            _issueHistory = new IssueHistoryPath(this, null, Keys.ISSUE_HISTORY__FK_ISSUE_HISTORY_TENANT.getInverseKey());

        return _issueHistory;
    }

    private transient IssueLabelPath _issueLabel;

    /**
     * Get the implicit to-many join path to the <code>public.issue_label</code>
     * table
     */
    public IssueLabelPath issueLabel() {
        if (_issueLabel == null)
            _issueLabel = new IssueLabelPath(this, null, Keys.ISSUE_LABEL__FK_ISSUE_LABEL_TENANT.getInverseKey());

        return _issueLabel;
    }

    private transient IssuePath _issue;

    /**
     * Get the implicit to-many join path to the <code>public.issue</code> table
     */
    public IssuePath issue() {
        if (_issue == null)
            _issue = new IssuePath(this, null, Keys.ISSUE__FK_ISSUE_TENANT.getInverseKey());

        return _issue;
    }

    private transient IssueTypeDefPath _issueTypeDef;

    /**
     * Get the implicit to-many join path to the
     * <code>public.issue_type_def</code> table
     */
    public IssueTypeDefPath issueTypeDef() {
        if (_issueTypeDef == null)
            _issueTypeDef = new IssueTypeDefPath(this, null, Keys.ISSUE_TYPE_DEF__FK_ISSUE_TYPE_DEF_TENANT.getInverseKey());

        return _issueTypeDef;
    }

    private transient IssueWatcherPath _issueWatcher;

    /**
     * Get the implicit to-many join path to the
     * <code>public.issue_watcher</code> table
     */
    public IssueWatcherPath issueWatcher() {
        if (_issueWatcher == null)
            _issueWatcher = new IssueWatcherPath(this, null, Keys.ISSUE_WATCHER__FK_ISSUE_WATCHER_TENANT.getInverseKey());

        return _issueWatcher;
    }

    private transient LabelPath _label;

    /**
     * Get the implicit to-many join path to the <code>public.label</code> table
     */
    public LabelPath label() {
        if (_label == null)
            _label = new LabelPath(this, null, Keys.LABEL__FK_LABEL_TENANT.getInverseKey());

        return _label;
    }

    private transient MessageAttachmentPath _messageAttachment;

    /**
     * Get the implicit to-many join path to the
     * <code>public.message_attachment</code> table
     */
    public MessageAttachmentPath messageAttachment() {
        if (_messageAttachment == null)
            _messageAttachment = new MessageAttachmentPath(this, null, Keys.MESSAGE_ATTACHMENT__FK_MESSAGE_ATTACHMENT_TENANT.getInverseKey());

        return _messageAttachment;
    }

    private transient MessageReactionPath _messageReaction;

    /**
     * Get the implicit to-many join path to the
     * <code>public.message_reaction</code> table
     */
    public MessageReactionPath messageReaction() {
        if (_messageReaction == null)
            _messageReaction = new MessageReactionPath(this, null, Keys.MESSAGE_REACTION__FK_MESSAGE_REACTION_TENANT.getInverseKey());

        return _messageReaction;
    }

    private transient MessagePath _message;

    /**
     * Get the implicit to-many join path to the <code>public.message</code>
     * table
     */
    public MessagePath message() {
        if (_message == null)
            _message = new MessagePath(this, null, Keys.MESSAGE__FK_MESSAGE_TENANT.getInverseKey());

        return _message;
    }

    private transient MilestonePath _milestone;

    /**
     * Get the implicit to-many join path to the <code>public.milestone</code>
     * table
     */
    public MilestonePath milestone() {
        if (_milestone == null)
            _milestone = new MilestonePath(this, null, Keys.MILESTONE__FK_MILESTONE_TENANT.getInverseKey());

        return _milestone;
    }

    private transient NotificationPath _notification;

    /**
     * Get the implicit to-many join path to the
     * <code>public.notification</code> table
     */
    public NotificationPath notification() {
        if (_notification == null)
            _notification = new NotificationPath(this, null, Keys.NOTIFICATION__FK_NOTIFICATION_TENANT.getInverseKey());

        return _notification;
    }

    private transient ProjectIssueSequencePath _projectIssueSequence;

    /**
     * Get the implicit to-many join path to the
     * <code>public.project_issue_sequence</code> table
     */
    public ProjectIssueSequencePath projectIssueSequence() {
        if (_projectIssueSequence == null)
            _projectIssueSequence = new ProjectIssueSequencePath(this, null, Keys.PROJECT_ISSUE_SEQUENCE__FK_PROJECT_ISSUE_SEQUENCE_TENANT.getInverseKey());

        return _projectIssueSequence;
    }

    private transient ProjectMemberPath _projectMember;

    /**
     * Get the implicit to-many join path to the
     * <code>public.project_member</code> table
     */
    public ProjectMemberPath projectMember() {
        if (_projectMember == null)
            _projectMember = new ProjectMemberPath(this, null, Keys.PROJECT_MEMBER__FK_PROJECT_MEMBER_TENANT.getInverseKey());

        return _projectMember;
    }

    private transient ProjectPath _project;

    /**
     * Get the implicit to-many join path to the <code>public.project</code>
     * table
     */
    public ProjectPath project() {
        if (_project == null)
            _project = new ProjectPath(this, null, Keys.PROJECT__FK_PROJECT_TENANT.getInverseKey());

        return _project;
    }

    private transient RolePermissionPath _rolePermission;

    /**
     * Get the implicit to-many join path to the
     * <code>public.role_permission</code> table
     */
    public RolePermissionPath rolePermission() {
        if (_rolePermission == null)
            _rolePermission = new RolePermissionPath(this, null, Keys.ROLE_PERMISSION__FK_ROLE_PERMISSION_TENANT.getInverseKey());

        return _rolePermission;
    }

    private transient RolePath _role;

    /**
     * Get the implicit to-many join path to the <code>public.role</code> table
     */
    public RolePath role() {
        if (_role == null)
            _role = new RolePath(this, null, Keys.ROLE__FK_ROLE_TENANT.getInverseKey());

        return _role;
    }

    private transient SavedViewPath _savedView;

    /**
     * Get the implicit to-many join path to the <code>public.saved_view</code>
     * table
     */
    public SavedViewPath savedView() {
        if (_savedView == null)
            _savedView = new SavedViewPath(this, null, Keys.SAVED_VIEW__FK_SAVED_VIEW_TENANT.getInverseKey());

        return _savedView;
    }

    private transient UserGroupMemberPath _userGroupMember;

    /**
     * Get the implicit to-many join path to the
     * <code>public.user_group_member</code> table
     */
    public UserGroupMemberPath userGroupMember() {
        if (_userGroupMember == null)
            _userGroupMember = new UserGroupMemberPath(this, null, Keys.USER_GROUP_MEMBER__FK_USER_GROUP_MEMBER_TENANT.getInverseKey());

        return _userGroupMember;
    }

    private transient UserGroupPath _userGroup;

    /**
     * Get the implicit to-many join path to the <code>public.user_group</code>
     * table
     */
    public UserGroupPath userGroup() {
        if (_userGroup == null)
            _userGroup = new UserGroupPath(this, null, Keys.USER_GROUP__FK_USER_GROUP_TENANT.getInverseKey());

        return _userGroup;
    }

    private transient UserPriorityItemPath _userPriorityItem;

    /**
     * Get the implicit to-many join path to the
     * <code>public.user_priority_item</code> table
     */
    public UserPriorityItemPath userPriorityItem() {
        if (_userPriorityItem == null)
            _userPriorityItem = new UserPriorityItemPath(this, null, Keys.USER_PRIORITY_ITEM__FK_USER_PRIORITY_ITEM_TENANT.getInverseKey());

        return _userPriorityItem;
    }

    private transient UserRolePath _userRole;

    /**
     * Get the implicit to-many join path to the <code>public.user_role</code>
     * table
     */
    public UserRolePath userRole() {
        if (_userRole == null)
            _userRole = new UserRolePath(this, null, Keys.USER_ROLE__FK_USER_ROLE_TENANT.getInverseKey());

        return _userRole;
    }

    private transient WorkspaceAssistantPath _workspaceAssistant;

    /**
     * Get the implicit to-many join path to the
     * <code>public.workspace_assistant</code> table
     */
    public WorkspaceAssistantPath workspaceAssistant() {
        if (_workspaceAssistant == null)
            _workspaceAssistant = new WorkspaceAssistantPath(this, null, Keys.WORKSPACE_ASSISTANT__FK_WORKSPACE_ASSISTANT_TENANT.getInverseKey());

        return _workspaceAssistant;
    }

    private transient MembershipPath _membership;

    /**
     * Get the implicit to-many join path to the <code>public.membership</code>
     * table
     */
    public MembershipPath membership() {
        if (_membership == null)
            _membership = new MembershipPath(this, null, Keys.MEMBERSHIP__MEMBERSHIP_TENANT_ID_FKEY.getInverseKey());

        return _membership;
    }

    private transient ThreadReadStatePath _threadReadState;

    /**
     * Get the implicit to-many join path to the
     * <code>public.thread_read_state</code> table
     */
    public ThreadReadStatePath threadReadState() {
        if (_threadReadState == null)
            _threadReadState = new ThreadReadStatePath(this, null, Keys.THREAD_READ_STATE__THREAD_READ_STATE_TENANT_ID_FKEY.getInverseKey());

        return _threadReadState;
    }

    private transient UserApiTokenPath _userApiToken;

    /**
     * Get the implicit to-many join path to the
     * <code>public.user_api_token</code> table
     */
    public UserApiTokenPath userApiToken() {
        if (_userApiToken == null)
            _userApiToken = new UserApiTokenPath(this, null, Keys.USER_API_TOKEN__USER_API_TOKEN_TENANT_ID_FKEY.getInverseKey());

        return _userApiToken;
    }

    private transient UserDashboardPath _userDashboard;

    /**
     * Get the implicit to-many join path to the
     * <code>public.user_dashboard</code> table
     */
    public UserDashboardPath userDashboard() {
        if (_userDashboard == null)
            _userDashboard = new UserDashboardPath(this, null, Keys.USER_DASHBOARD__USER_DASHBOARD_TENANT_ID_FKEY.getInverseKey());

        return _userDashboard;
    }

    private transient WikiPageAttachmentPath _wikiPageAttachment;

    /**
     * Get the implicit to-many join path to the
     * <code>public.wiki_page_attachment</code> table
     */
    public WikiPageAttachmentPath wikiPageAttachment() {
        if (_wikiPageAttachment == null)
            _wikiPageAttachment = new WikiPageAttachmentPath(this, null, Keys.WIKI_PAGE_ATTACHMENT__WIKI_PAGE_ATTACHMENT_TENANT_ID_FKEY.getInverseKey());

        return _wikiPageAttachment;
    }

    private transient WikiPagePath _wikiPage;

    /**
     * Get the implicit to-many join path to the <code>public.wiki_page</code>
     * table
     */
    public WikiPagePath wikiPage() {
        if (_wikiPage == null)
            _wikiPage = new WikiPagePath(this, null, Keys.WIKI_PAGE__WIKI_PAGE_TENANT_ID_FKEY.getInverseKey());

        return _wikiPage;
    }

    private transient WikiReferencePath _wikiReference;

    /**
     * Get the implicit to-many join path to the
     * <code>public.wiki_reference</code> table
     */
    public WikiReferencePath wikiReference() {
        if (_wikiReference == null)
            _wikiReference = new WikiReferencePath(this, null, Keys.WIKI_REFERENCE__WIKI_REFERENCE_TENANT_ID_FKEY.getInverseKey());

        return _wikiReference;
    }

    private transient WikiRevisionPath _wikiRevision;

    /**
     * Get the implicit to-many join path to the
     * <code>public.wiki_revision</code> table
     */
    public WikiRevisionPath wikiRevision() {
        if (_wikiRevision == null)
            _wikiRevision = new WikiRevisionPath(this, null, Keys.WIKI_REVISION__WIKI_REVISION_TENANT_ID_FKEY.getInverseKey());

        return _wikiRevision;
    }

    private transient WikiSpaceMemberPath _wikiSpaceMember;

    /**
     * Get the implicit to-many join path to the
     * <code>public.wiki_space_member</code> table
     */
    public WikiSpaceMemberPath wikiSpaceMember() {
        if (_wikiSpaceMember == null)
            _wikiSpaceMember = new WikiSpaceMemberPath(this, null, Keys.WIKI_SPACE_MEMBER__WIKI_SPACE_MEMBER_TENANT_ID_FKEY.getInverseKey());

        return _wikiSpaceMember;
    }

    private transient WikiSpacePath _wikiSpace;

    /**
     * Get the implicit to-many join path to the <code>public.wiki_space</code>
     * table
     */
    public WikiSpacePath wikiSpace() {
        if (_wikiSpace == null)
            _wikiSpace = new WikiSpacePath(this, null, Keys.WIKI_SPACE__WIKI_SPACE_TENANT_ID_FKEY.getInverseKey());

        return _wikiSpace;
    }

    private transient WorkerJobPath _workerJob;

    /**
     * Get the implicit to-many join path to the <code>public.worker_job</code>
     * table
     */
    public WorkerJobPath workerJob() {
        if (_workerJob == null)
            _workerJob = new WorkerJobPath(this, null, Keys.WORKER_JOB__WORKER_JOB_TENANT_ID_FKEY.getInverseKey());

        return _workerJob;
    }

    /**
     * Get the implicit many-to-many join path to the <code>public.user</code>
     * table, via the <code>membership_user_id_fkey</code> key
     */
    public UserPath membershipUserIdFkey() {
        return membership().user();
    }

    /**
     * Get the implicit many-to-many join path to the <code>public.user</code>
     * table, via the <code>user_dashboard_user_id_fkey</code> key
     */
    public UserPath userDashboardUserIdFkey() {
        return userDashboard().user();
    }

    @Override
    public List<Check<TenantRecord>> getChecks() {
        return Arrays.asList(
            Internal.createCheck(this, DSL.name("tenant_status_check"), "(((status)::text = ANY ((ARRAY['ACTIVE'::character varying, 'SUSPENDED'::character varying])::text[])))", true)
        );
    }

    @Override
    public Tenant as(String alias) {
        return new Tenant(DSL.name(alias), this);
    }

    @Override
    public Tenant as(Name alias) {
        return new Tenant(alias, this);
    }

    @Override
    public Tenant as(Table<?> alias) {
        return new Tenant(alias.getQualifiedName(), this);
    }

    /**
     * Rename this table
     */
    @Override
    public Tenant rename(String name) {
        return new Tenant(DSL.name(name), null);
    }

    /**
     * Rename this table
     */
    @Override
    public Tenant rename(Name name) {
        return new Tenant(name, null);
    }

    /**
     * Rename this table
     */
    @Override
    public Tenant rename(Table<?> name) {
        return new Tenant(name.getQualifiedName(), null);
    }

    /**
     * Create an inline derived table from this table
     */
    @Override
    public Tenant where(Condition condition) {
        return new Tenant(getQualifiedName(), aliased() ? this : null, null, condition);
    }

    /**
     * Create an inline derived table from this table
     */
    @Override
    public Tenant where(Collection<? extends Condition> conditions) {
        return where(DSL.and(conditions));
    }

    /**
     * Create an inline derived table from this table
     */
    @Override
    public Tenant where(Condition... conditions) {
        return where(DSL.and(conditions));
    }

    /**
     * Create an inline derived table from this table
     */
    @Override
    public Tenant where(Field<Boolean> condition) {
        return where(DSL.condition(condition));
    }

    /**
     * Create an inline derived table from this table
     */
    @Override
    @PlainSQL
    public Tenant where(SQL condition) {
        return where(DSL.condition(condition));
    }

    /**
     * Create an inline derived table from this table
     */
    @Override
    @PlainSQL
    public Tenant where(@Stringly.SQL String condition) {
        return where(DSL.condition(condition));
    }

    /**
     * Create an inline derived table from this table
     */
    @Override
    @PlainSQL
    public Tenant where(@Stringly.SQL String condition, Object... binds) {
        return where(DSL.condition(condition, binds));
    }

    /**
     * Create an inline derived table from this table
     */
    @Override
    @PlainSQL
    public Tenant where(@Stringly.SQL String condition, QueryPart... parts) {
        return where(DSL.condition(condition, parts));
    }

    /**
     * Create an inline derived table from this table
     */
    @Override
    public Tenant whereExists(Select<?> select) {
        return where(DSL.exists(select));
    }

    /**
     * Create an inline derived table from this table
     */
    @Override
    public Tenant whereNotExists(Select<?> select) {
        return where(DSL.notExists(select));
    }
}
