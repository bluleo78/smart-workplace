import type { IssueFilters } from '@/types/issue';

// 체크리스트 뷰 스텁 — Task 2에서 실제 구현으로 교체.
// eslint-disable-next-line @typescript-eslint/no-unused-vars
export function PersonalChecklistView(_props: { projectKey: string; filters: IssueFilters }) {
  return <div data-testid="personal-checklist" className="min-h-[1px]" />;
}
