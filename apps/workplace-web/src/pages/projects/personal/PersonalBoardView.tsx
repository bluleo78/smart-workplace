import type { IssueFilters } from '@/types/issue';

// 보드 뷰 스텁 — Task 3에서 실제 구현으로 교체.
// eslint-disable-next-line @typescript-eslint/no-unused-vars
export function PersonalBoardView(_props: { projectKey: string; filters: IssueFilters }) {
  return <div data-testid="personal-board" />;
}
