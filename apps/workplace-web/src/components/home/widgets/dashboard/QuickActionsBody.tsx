// 대시보드 그리드 위젯 어댑터 — QuickActions(새 이슈·메일 작성·새 대화 버튼 행)를 registry Component
// 시그니처에 맞춰 감싼다. count 무시.
import { QuickActions } from '../../QuickActions'

export default function QuickActionsBody() {
  return <QuickActions />
}
