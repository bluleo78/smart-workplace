// 공개 채널 탐색 모달 — T4 에서 구현. 현재는 컴파일용 스텁.
export function ChannelBrowser({
  open,
  onOpenChange,
}: {
  open: boolean
  onOpenChange: (v: boolean) => void
}) {
  void open
  void onOpenChange
  return null
}
