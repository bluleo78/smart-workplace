{
  "name": "smart-workplace",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "build": "turbo run build",
    "dev": "node scripts/free-ports.mjs 9090 7070 6173 && turbo run dev",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "clean": "turbo run clean",
    "db:up": "docker compose up -d",
    "db:down": "docker compose down",
    "db:reset": "docker compose down -v && docker compose up -d",
    "db:logs": "docker compose logs -f db",
    "prepare": "husky"
  },
  "//lint-staged-note": "Java 포맷은 Spotless 가 gradle test 내 spotlessCheck 로 검증. apps/workplace-web 추가 시 eslint 추가.",
  "lint-staged": {
    "apps/workplace-web/{src,e2e}/**/*.{ts,tsx}": [
      "echo 'workplace-web 셋업 후 eslint 추가 예정'"
    ]
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^17.0.5",
    "turbo": "^2.5.0"
  },
  "packageManager": "pnpm@10.11.0"
}
